# soft global overrides
[ -z "$ANDROID_HOME" ] && {
  export ANDROID_HOME=<%= scope.lookupvar 'homebrew::config::installdir' %>/opt/android
}
[ -z "$ANDROID_SDK_ROOT" ] && {
  export ANDROID_SDK_ROOT=$ANDROID_HOME
}